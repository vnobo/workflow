app.server.ip: 127.0.0.1

logging.level:
  com.celesea.activiti.*: debug

server:
  port: 8080
  shutdown: graceful

spring:
  application.name: celesea-activiti
  cloud:
    loadbalancer.ribbon.enabled: false
    nacos.config:
      file-extension: yaml
      server-addr: ${app.server.ip}:8848
  datasource:
    url: jdbc:postgresql://${app.server.ip}:5432/celesea
    username: farmer
    password: q1w2e3..
    driver-class-name: org.postgresql.Driver
  rabbitmq:
    host: ${app.server.ip}
    username: farmer
    password: q1w2e3..
    listener.simple.retry:
      enabled: false
      max-attempts: 5
      initial-interval: 2s
      max-interval: 10s
      multiplier: 2
  task.execution.pool:
    keep-alive: 10s
  codec:
    log-request-details: true
  activiti:
    security:
      enabled: false

springfox.documentation.swagger-ui.base-url: /documentation